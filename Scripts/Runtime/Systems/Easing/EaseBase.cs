using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SorryLab.Easing {
    abstract class EaseBase {
        protected const double TWO_PI = Mathf.PI * 2;
        protected const double HALF_PI = Mathf.PI / 2;
    }
}
